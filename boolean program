#include <stdio.h>
#include <stdbool.h>
int main()
{
    bool x= false;
    if(x==true){
        printf("value of x is true");
    }
    else{
        printf("value of x is false");
    }

    return 0;
}
